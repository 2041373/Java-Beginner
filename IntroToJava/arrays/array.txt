Arrays

An array is a container object that holds a fixed number of values ​​of one type. The length of the field is determined when the field is created. Once created, its length is fixed. You’ve already seen an example of fields in the main method of the “Hello World!” application

